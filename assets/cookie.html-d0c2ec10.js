import{_ as e}from"./plugin-vue_export-helper-c27b6911.js";import{o as i,c as o,a as l,b as c,f as a}from"./app-af427ef1.js";const d={},t=c("p",null,"HTTP cookie 通常也叫作cookie，最初用于在客户端存储会话信息。这个规范要求服务器在响应 HTTP 请求时，通过发送Set-Cookie HTTP 头部包含会话信息。",-1),n=a(`<p>下面是包含这个头部的一个HTTP响应：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>HTTP/1.1 200 OK
Content-type: text/html
Set-Cookie: name=value
Other-header: other-header-value
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这个HTTP 响应会设置一个名为&quot;name&quot;，值为&quot;value&quot;的cookie。名和值在发送时都会经过URL编码。浏览器会存储这些会话信息，并在之后的每个请求中都会通过HTTP 头部cookie 再将它们发回服务器，比如：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>GET /index.jsl HTTP/1.1
Cookie: name=value
Other-header: other-header-value
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这些发送回服务器的额外信息可用于唯一标识发送请求的客户端。</p><h2 id="限制" tabindex="-1"><a class="header-anchor" href="#限制" aria-hidden="true">#</a> 限制</h2><p>cookie 是与特定域绑定的。设置cookie后，它会与请求一起发送到创建它的域。这个限制能保证cookie中存储的信息只被认可的接收者开放，不被其它域访问。</p><p>因为cookie存储在客户端机器上，所以未保证它不会被恶意利用，浏览器会施加限制。同时，cookie也不会占用太多磁盘空间。<br> 通常，只要遵循以下大致的限制，就不会在任何浏览器中碰到问题：</p><ul><li>不超过300个cookie</li><li>每个cookie不超过4096字节</li><li>每个域不超过20个cookie</li><li>每个域不超过81920字节。</li></ul><p>每个域能设置的cookie总数也是受限的，但不同浏览器的限制不同。例如：</p><ul><li>最新版IE和Edge限制每个域不超过50个cookie；</li><li>最新版Firefox限制每个域不超过150个cookie；</li><li>最新版Opera限制每个域不超过180个cookie；</li><li>Safari和chrome对每个域的cookie数没有硬性限制。</li></ul><p>如果cookie 总数超过了单个域的上限，浏览器就会删除之前设置的cookie：</p><ul><li>IE 和Opera 会按照最近最少使用（LRU，Least Recently Used）原则删除之前的cookie，以便为新设置的cookie 腾出空间。</li><li>Firefox好像会随机删除之前的cookie</li></ul><p><mark>大多数浏览器对cookie 的限制是不超过4096 字节，上下可以有一个字节的误差。为跨浏览器兼容，最好保证cookie 的大小不超过4095 字节。这个大小限制适用于一个域的所有cookie，而不是单个cookie。</mark></p><h2 id="cookie构成" tabindex="-1"><a class="header-anchor" href="#cookie构成" aria-hidden="true">#</a> cookie构成</h2><ul><li>名称：唯一标识cookie的名称，cookie名不区分大小写，因此<code>myCookie</code>和<code>MyCookie</code>是同一个名称。不过，实践中最好将cookie名当成区分大小写来对待，因为一些服务器软件可能这样对待它们。cookie名必须经过URL编码。</li><li>值：存储在cookie里的字符串值。这个值必须经过URL编码。</li><li>域</li><li>路径</li><li>过期时间</li><li>安全标志</li></ul>`,16);function r(s,k){return i(),o("div",null,[t,l(" more "),n])}const h=e(d,[["render",r],["__file","cookie.html.vue"]]);export{h as default};
